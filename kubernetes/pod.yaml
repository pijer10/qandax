apiVersion: v1
kind: Pod
metadata:
  name: simple-pod
  namespace: qandax
  labels:
    app: my-app
spec:
  containers:
  - name: my-container
    image: nginx:latest
    ports:
    - containerPort: 80
